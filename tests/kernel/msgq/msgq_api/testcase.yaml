tests:
  kernel.message_queue:
    tags:
      - kernel
      - userspace
  kernel.message_queue.put_front:
    tags:
      - kernel
      - userspace
    extra_configs:
      - CONFIG_TEST_MSGQ_PUT_FRONT=y
